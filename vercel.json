{
  "framework": "vite",
  "buildCommand": "pnpm --filter @sg/shared build && pnpm --filter @sg/web build",
  "outputDirectory": "apps/web/dist",
  "installCommand": "pnpm install",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
